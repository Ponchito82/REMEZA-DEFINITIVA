<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Mismo fondo que la app: si no, el degradado se corta en seco
             contra el gris de la barra de navegacion. -->
        <item name="android:statusBarColor">#03051D</item>
        <item name="android:navigationBarColor">#03051D</item>
    </style>

</resources>
